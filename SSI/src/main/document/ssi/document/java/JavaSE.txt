Java中String.format使用笔记
System.out.println(String.format("%-10s %5s", "ac","12ew3"));
显示结果：ac         12ew3
其中：
%-10s表示字符串"ac"左对齐占用十个字符的长度，不足的部分以空格填充；

%5s表示字符串"12ew3"右对齐占用五个字符的长度，不足的部分以空格填充；

ac与12ew3之间包含9个空格，其中八个空格是"ac"不足十个字符而补足的，另外一个是%-10s与%5s之间的那个空格；若%-10s %5s为","，则显示结果为:ac        ,12ew3